#!/usr/bin/env bash

set -e

read -p "Are you sure? " -n 1 -r

if [[ $REPLY =~ ^[Yy]$ ]]
then
    docker-compose stop
    docker-compose rm -v --all --force
    docker-compose build
    docker-compose up -d
    docker-compose logs -f
fi
